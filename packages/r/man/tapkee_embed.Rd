% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embed.R
\name{tapkee_embed}
\alias{tapkee_embed}
\title{Dimensionality Reduction with Tapkee}
\usage{
tapkee_embed(
  data,
  method = "lle",
  neighbors_method = NULL,
  eigen_method = NULL,
  num_neighbors = NULL,
  target_dimension = NULL,
  gaussian_kernel_width = NULL,
  landmark_ratio = NULL,
  max_iteration = NULL,
  diffusion_map_timesteps = NULL,
  sne_perplexity = NULL,
  sne_theta = NULL,
  squishing_rate = NULL,
  spe_global_strategy = NULL,
  spe_num_updates = NULL,
  spe_tolerance = NULL,
  nullspace_shift = NULL,
  klle_shift = NULL,
  fa_epsilon = NULL,
  check_connectivity = NULL
)
}
\arguments{
\item{data}{A numeric matrix where rows are features and columns are
samples. Each column is one data point.}

\item{method}{Character string specifying the reduction method. Short names:
\code{"lle"}, \code{"isomap"}, \code{"pca"}, \code{"t-sne"},
\code{"mds"}, \code{"dm"}, \code{"kpca"}, \code{"la"}, \code{"lpp"},
\code{"npe"}, \code{"ltsa"}, \code{"lltsa"}, \code{"hlle"},
\code{"l-mds"}, \code{"l-isomap"}, \code{"spe"}, \code{"fa"},
\code{"random_projection"}, \code{"manifold_sculpting"}, \code{"passthru"}.}

\item{neighbors_method}{Character string specifying the neighbor search method:
\code{"brute"} (brute-force), \code{"vptree"} (vantage point tree),
\code{"covertree"} (cover tree, if available). Default: library default.}

\item{eigen_method}{Character string specifying the eigendecomposition method:
\code{"dense"}, \code{"randomized"}, \code{"arpack"} (if available).
Default: library default.}

\item{num_neighbors}{Integer. Number of nearest neighbors for local methods.
Default in Tapkee: 5.}

\item{target_dimension}{Integer. Output dimensionality.
Default in Tapkee: 2.}

\item{gaussian_kernel_width}{Numeric. Kernel width for Laplacian Eigenmaps,
Diffusion Maps, LPP. Default in Tapkee: 1.0.}

\item{landmark_ratio}{Numeric in \eqn{[0, 1]}. Ratio of landmarks for
Landmark Isomap and Landmark MDS. Default in Tapkee: 0.5.}

\item{max_iteration}{Integer. Maximum iterations for iterative methods.
Default in Tapkee: 100.}

\item{diffusion_map_timesteps}{Integer. Number of timesteps for Diffusion
Maps. Default in Tapkee: 3.}

\item{sne_perplexity}{Numeric. Perplexity for t-SNE.
Default in Tapkee: 30.0.}

\item{sne_theta}{Numeric. Barnes-Hut theta for t-SNE approximation.
Default in Tapkee: 0.5.}

\item{squishing_rate}{Numeric. Rate for Manifold Sculpting.
Default in Tapkee: 0.99.}

\item{spe_global_strategy}{Logical. Use global strategy for SPE.
Default in Tapkee: \code{TRUE}.}

\item{spe_num_updates}{Integer. Number of updates for SPE.
Default in Tapkee: 100.}

\item{spe_tolerance}{Numeric. Convergence tolerance for SPE.
Default in Tapkee: 1e-9.}

\item{nullspace_shift}{Numeric. Diagonal shift for eigenproblems.
Default in Tapkee: 1e-9.}

\item{klle_shift}{Numeric. KLLE regularizer.
Default in Tapkee: 1e-3.}

\item{fa_epsilon}{Numeric. Factor Analysis convergence epsilon.
Default in Tapkee: 1e-9.}

\item{check_connectivity}{Logical. Check neighborhood graph connectivity.
Default in Tapkee: \code{TRUE}.}
}
\value{
A numeric matrix with \code{ncol(data)} rows and
  \code{target_dimension} columns. Each row is the embedded
  representation of the corresponding input sample.
}
\description{
Embed high-dimensional data into a lower-dimensional space using
one of 20+ algorithms from the Tapkee C++ library.
}
\examples{
set.seed(42)
X <- matrix(rnorm(300), nrow = 3, ncol = 100)
embedding <- tapkee_embed(X, method = "pca", target_dimension = 2L)
plot(embedding[, 1], embedding[, 2], main = "PCA embedding")

}
